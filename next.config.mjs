/** @type {import('next').NextConfig} */
const nextConfig = {
  // Durable data and uploads are handled by the backend; no secrets belong here.
  reactStrictMode: true,
};

export default nextConfig;
